/**
 * Visual identity for the white-label deployment. Model providers remain an
 * implementation detail and must not determine user-facing names or artwork.
 */
export const deploymentBrand = {
  assistantName: 'ÁrvorePress IA',
  assistantIconURL: '/assets/arvorepress-icon.png',
} as const;
